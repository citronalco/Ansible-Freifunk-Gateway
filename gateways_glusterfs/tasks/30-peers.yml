# TODO:
# nur ab 3 Gateways!
# alte peers löschen!
# alte Volumes auflösen
# alte Bricks rausschmeissen
# alte Images löschen

# gluster volume <VOLNAME> remove-brick replica <COUNT> <BRICK> ...

# Cluster-Pool
# Auf allen Rechnern laufen lassen, damit der "servers.freifunk-domain"-Name als "other Name" drin steht! Sonst fehlt der von dem Rechner, auf dem das Kommando ausgeführt wird.
# Eigenen Rechner rausfiltern?
- name: Alle Gateways zum Pool hinzufügen (auf dem ersten Server)
  gluster_peer:
    state: present
    nodes: "{{ groups['gateways'] | product(['.servers.' + freifunk.domain ]) | map('join') }} "
  run_once: yes
  #when: inventory_hostname == groups['gateways'][0]
 # Eigenen Rechner rausfiltern?
